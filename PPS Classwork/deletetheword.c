#include<stdio.h>
#include<string.h>
int main()
{
    char s[700];
    printf("Enter a string: ");
    gets(s);
    printf("Enter the word which you want to remove from the string: ");
    return 0;
}